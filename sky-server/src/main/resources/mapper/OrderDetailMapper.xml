<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insertBatch">
        insert into order_detail(order_id, dish_id, setmeal_id, name, image, dish_flavor, number, amount)
        values
        <foreach collection="orderDetailList" item="item" separator=",">
            (#{item.orderId}, #{item.dishId}, #{item.setmealId}, #{item.name}, #{item.image}, #{item.dishFlavor}, #{item.number}, #{item.amount})
        </foreach>
    </insert>
    <select id="listTop10ByDate" resultType="java.util.Map">
        select name as name, sum(number) as value
        from order_detail
        where dish_id in (
        select distinct dish_id
        from order_detail
        where order_id in
        <foreach collection="orderIds" item="orderId" open="(" separator="," close=")">
            #{orderId}
        </foreach>
        )
        group by name
        order by value desc
        limit 10
    </select>
</mapper>
